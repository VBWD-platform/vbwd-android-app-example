package com.vbwd.app

import org.junit.jupiter.api.Assertions.assertTrue
import org.junit.jupiter.api.Test

/**
 * A01.0 smoke test — proves the `:app` JUnit5 unit-test runner compiles + runs.
 */
class AppSmokeTest {
    @Test
    fun `test runner works`() {
        assertTrue(true)
    }
}
